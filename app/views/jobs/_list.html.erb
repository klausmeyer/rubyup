<table class="table table-striped">
  <colgroup>
    <col width="20%">
    <col width="40%">
    <col width="20%">
    <col width="20%">
  </colgroup>
  <tr>
    <th>Repo</th>
    <th>Name</th>
    <th>State</th>
    <th>Actions</th>
  </tr>
  <% if @jobs.any? %>
    <% @jobs.each do |job| %>
      <tr>
        <td><%= job.repository.name %></td>
        <td><%= job.name %></td>
        <td><%= job_state_badge job.state %></td>
        <td><%= link_to 'Show', job, class: 'btn btn-sm btn-secondary' %></td>
      </tr>
    <% end %>
  <% else %>
    <tr>
      <td colspan="4" class="text-center">
        No records found.
      </td>
    </tr>
  <% end %>
</table>
